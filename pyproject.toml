[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
# Conservative default rule set: pyflakes + a subset of pycodestyle errors.
# Broaden deliberately, not accidentally.
line-length = 110

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
